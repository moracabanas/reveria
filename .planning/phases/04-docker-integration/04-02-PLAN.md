---
phase: "04"
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - DOCKER-03
  - DOCKER-04

must_haves:
  truths:
    - "docker-compose.yml orchestrates backend and frontend"
    - "Services communicate via internal network"
    - "Frontend can reach backend API"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Multi-service orchestration for backend + frontend"
      min_lines: 30
  key_links:
    - from: "docker-compose.yml"
      to: "frontend/Dockerfile"
      via: "frontend service build context"
      pattern: "frontend.*build"
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "backend service build context"
      pattern: "backend.*build"
    - from: "frontend"
      to: "backend"
      via: "NEXT_PUBLIC_BACKEND_URL environment variable"
      pattern: "NEXT_PUBLIC_BACKEND_URL"

---

<objective>
Create complete docker-compose.yml orchestrating backend and frontend services.

Purpose: Enable end-to-end running of the application in Docker with proper service communication.
Output: Working docker-compose.yml with backend + frontend services.
</objective>

<context>
@docker-compose.yml (existing)
@backend/Dockerfile
@frontend/Dockerfile (from 04-01)

## Service Communication
- Backend: localhost:8000 (FastAPI)
- Frontend: localhost:3000 (Next.js static)
- Frontend should proxy /api/* requests to backend
- Use Docker internal network for service-to-service communication

## Environment Variables
- Backend: PYTHONUNBUFFERED=1
- Frontend: NEXT_PUBLIC_BACKEND_URL=http://backend:8000

## Volumes (for development)
- Backend: mount app/ directory for code changes (optional)
- Frontend: mount for dev mode with volume mounts
</context>

<tasks>

<task type="auto">
  <name>task 1: Update docker-compose.yml with frontend service</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml, backend/Dockerfile</read_first>
  <action>
Update `docker-compose.yml` to include both backend and frontend services:

1. Backend service (existing):
   - build: ./backend
   - ports: "8000:8000"
   - environment: PYTHONUNBUFFERED=1
   - Optional: volume mount for hot reload during development

2. Frontend service (new):
   - build: ./frontend
   - ports: "3000:3000"
   - environment: NEXT_PUBLIC_BACKEND_URL=http://backend:8000
   - depends_on: backend (ensure backend starts first)
   - Optional: volume mount for dev mode

3. Network configuration:
   - Create internal network: reverso-network
   - Both services join reverso-network
   - Frontend accesses backend via http://backend:8000 (Docker DNS)

4. Health checks (optional):
   - Backend: curl http://localhost:8000/health
   - Frontend: curl http://localhost:3000

Development vs Production profiles:
- dev profile: volume mounts for live reloading
- prod profile: no volume mounts, optimized builds
</action>
  <verify>
    <automated>cd /Users/morac/Documents/deduce/projects/reveria && docker compose up --build -d && sleep 10 && curl -s http://localhost:8000/health | grep -q "healthy" && curl -s http://localhost:3000 | grep -q "html" && echo "INTEGRATION_PASSED"</automated>
  </verify>
  <done>docker-compose.yml orchestrates both services and they communicate correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend | Internal Docker network, unencrypted |
| External → Frontend | Port 3000 exposed to host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Internal network | mitigate | Internal network not exposed externally |
| T-04-05 | Denial of Service | Resource limits | mitigate | Add memory/CPU limits to services |
| T-04-06 | Elevation of Privilege | Volume mounts | mitigate | Dev mode only; production uses build artifacts |
</threat_model>

<verification>
1. Start: `docker compose up --build -d`
2. Check backend: `curl http://localhost:8000/health`
3. Check frontend: `curl http://localhost:3000`
4. View logs: `docker compose logs -f`
5. Stop: `docker compose down`
</verification>

<success_criteria>
- docker-compose.yml includes both backend and frontend
- Both services build and start successfully
- Frontend can communicate with backend via internal DNS
- Ports 8000 and 3000 accessible from host
</success_criteria>

<output>
After completion, create `.planning/phases/04-docker-integration/04-02-SUMMARY.md`
</output>
