---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/tests/__init__.py
  - backend/tests/test_model_loader.py
  - backend/tests/test_health.py
  - backend/app/main.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02

must_haves:
  truths:
    - "Tests verify model loader works on CPU fallback"
    - "Health endpoint returns correct status"
    - "Test suite passes"
  artifacts:
    - path: "backend/tests/test_model_loader.py"
      provides: "Unit tests for model loading"
      min_lines: 40
    - path: "backend/tests/test_health.py"
      provides: "Health endpoint tests"
      min_lines: 20
  key_links:
    - from: "backend/tests/"
      to: "backend/app/"
      via: "pytest imports"
      pattern: "from app import"

---

<objective>
Add unit tests for the foundation phase to verify model loading and health endpoint behavior.

Purpose: Ensure the foundation is stable and model loading works correctly with proper GPU/CPU fallback.

Output: Test suite covering:
- Model loader GPU/CPU detection
- Model loading on CPU fallback
- Health endpoint responses
</objective>

<context>
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/01-01-PLAN-SUMMARY.md
@backend/app/model_loader.py
@backend/app/main.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>task 1: Create test infrastructure</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_model_loader.py</files>
  <read_first>backend/app/model_loader.py, backend/app/config.py</read_first>
  <action>
Create test infrastructure for the backend:

1. Create `backend/tests/__init__.py` (empty)

2. Create `backend/tests/conftest.py` with pytest fixtures:
   - `mock_torch_cuda_available` - fixture that patches torch.cuda.is_available to return True
   - `mock_torch_cuda_unavailable` - fixture that patches torch.cuda.is_available to return False
   - `sample_signal` - fixture returning a numpy array of 1000 random floats for testing

3. Create `backend/tests/test_model_loader.py`:
   - Test ReversoModel initialization defaults to CPU when CUDA unavailable
   - Test ReversoModel device selection logic
   - Test model_loader.load_model() function behavior
   - Use pytest.mark.parametrize for testing both GPU-available and CPU-only scenarios
   - All tests should skip if actual model file is not available (use mock)
</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_model_loader.py -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <done>Test suite runs and passes for model loader scenarios</done>
</task>

<task type="auto">
  <name>task 2: Test health endpoint</name>
  <files>backend/tests/test_health.py</files>
  <read_first>backend/app/main.py</read_first>
  <action>
Create tests for the health endpoint:

1. Create `backend/tests/test_health.py`:
   - Test GET /health returns 200
   - Test GET /health response body contains "status" and "model_loaded" keys
   - Test health endpoint works when model is not loaded (returns model_loaded=False)
   - Test health endpoint works when model is loaded (returns model_loaded=True)
   - Use FastAPI TestClient for testing
</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_health.py -v --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>Health endpoint tests pass with 200 responses and correct JSON structure</done>
</task>

</tasks>

<verification>
Run full test suite: `cd backend && uv run pytest tests/ -v`
</verification>

<success_criteria>
- All model loader tests pass (GPU/CPU detection works)
- All health endpoint tests pass
- Test coverage includes error path for CUDA unavailability
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-PLAN-SUMMARY.md`
</output>
